<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://code.jquery.com/jquery-3.2.1.min.js" integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTqlAQgxVSNgt4=" crossorigin="anonymous">
  </script>
    {{>bootstrap}}
    <style>
       header{
            padding: 10px;
       }
       .link{
            font-weight: bolder;
            color: black;
            font-size: 15px;
            }
      .active{
            background-color: #EEEEEE;
            color: black;
            }
        body{
            background: #3d09fa linear-gradient(40deg,#24a9c0 0,#092dfa 100%) fixed !important;
            background-size: cover;
            
        }
        .error{
            color: red;
        }
        .login{
            padding: 30px;
            background: white;
            border-radius: 3px;
            text-align: center;
            width: 375px; 
            margin-top: 100px;
            margin-bottom: 100px;
        }
        .button{
            background: #fa2509;
            padding: 10px;
            font-size: 25px;
            color: white;
            width: 100%;
        }
        .button:hover{
             background: #770101;
             color: white;
       }
        .padding{
            padding: 10px;
        }
        .input{
            width: 100% ;
            height: 40px;
        }
    </style>
</head>
<body>
    <!--Header-->
    <div>
        {{>header}}
    </div>
      
    
    <div class="login container text-center">
        <a href="https://whc.ca"><img src="https://clients.whc.ca/templates/hwc-six/img/picto-whc.svg" alt=""></a>
        <h2>Sign in to WHC</h2>
        <form method="post" action="login_post" class="signin" role="aigninform">
            <div class="padding" id="emailError">
              {{#if data.error.email}}
                        <div class="alert alert-warning">
                            {{data.error.email}}
                        </div>
               {{/if}}
            <input class="input" type="text" name="email" id="email" placeholder="Email" autofocus="" value="{{#if data.values.email}}{{data.values.email}}{{/if}}">
            <br><br>
              {{#if data.error.password}}
                        <div class="alert alert-warning">
                            {{data.error.password}}
                        </div>
              {{/if}}
            <input class="input" type="password" name="password" id="Password" placeholder="Password" autocomplete="off">
            </div>
            <div class="padding text-center">
              <label><input type="checkbox" name="rememberMe"> Remember Me </label><br>
              <a href="https://clients.whc.ca/index.php?rp=/password/reset" class="text-center btn-block">Forgot Password?</a>
           </div>
           <input type="submit" class="button btn btn-lg btn-default" value="Log In">
           <div class="signup padding">
            New to WHC? <a href="registration" class=" text-secondary">Sign up</a>
          </div>    
        </form>
    </div>
</body>
</html>